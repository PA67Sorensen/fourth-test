name: Report Issues
on: issues
jobs:
  report:
    runs-on: ubuntu-latest
    steps:
      - name: Get code
        uses: actions/checkout@v3
      - name: Install dependencies
        run: npm ci
      - name: Report
        run: echo "${{ toJSON(github) }}"
